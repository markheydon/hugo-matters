<div class="flex h-full flex-col">
    <div class="flex h-14 items-center gap-2 border-b border-slate-200 px-4">
        <Icon Name="book-open" Size="24" CssClass="text-brand-600" />
        <span class="text-lg font-semibold text-slate-900">Hugo Matters</span>
    </div>

    <nav class="flex-1 space-y-1 p-3">
        <NavLink href="" Match="NavLinkMatch.All" class="nav-link" ActiveClass="nav-link-active">
            <Icon Name="home" Size="18" />
            Home
        </NavLink>
        <NavLink href="connect" class="nav-link" ActiveClass="nav-link-active">
            <Icon Name="link" Size="18" />
            Connect
        </NavLink>
        <NavLink href="session" class="nav-link" ActiveClass="nav-link-active">
            <Icon Name="git-branch" Size="18" />
            Session
        </NavLink>
        <NavLink href="content" class="nav-link" ActiveClass="nav-link-active">
            <Icon Name="file-text" Size="18" />
            Content
        </NavLink>
        <NavLink href="preview" class="nav-link" ActiveClass="nav-link-active">
            <Icon Name="eye" Size="18" />
            Preview
        </NavLink>
        <NavLink href="config" class="nav-link" ActiveClass="nav-link-active">
            <Icon Name="settings" Size="18" />
            Site config
        </NavLink>
    </nav>
</div>

<style>
    .nav-link {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        padding: 0.5rem 0.75rem;
        border-radius: 0.5rem;
        font-size: 0.875rem;
        font-weight: 500;
        color: rgb(71 85 105);
        text-decoration: none;
        transition: background-color 0.15s, color 0.15s;
    }

    .nav-link:hover {
        background-color: rgb(241 245 249);
        color: rgb(15 23 42);
    }

    .nav-link-active {
        background-color: rgb(240 249 255);
        color: rgb(2 132 199);
    }
</style>
